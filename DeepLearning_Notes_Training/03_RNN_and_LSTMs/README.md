# Recurrent Neural Networks (RNN & LSTM)
Before Transformers, RNNs and LSTMs were the kings of sequential data (text, time-series, audio). Covers the vanishing gradient problem and how LSTMs solve it.